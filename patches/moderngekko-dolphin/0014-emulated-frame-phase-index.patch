diff --git a/Source/Core/Common/FramePhaseTiming.h b/Source/Core/Common/FramePhaseTiming.h
--- a/Source/Core/Common/FramePhaseTiming.h
+++ b/Source/Core/Common/FramePhaseTiming.h
@@ -36,6 +36,7 @@ inline std::atomic<u64> s_static_recomp_cache_control_dcbi{0};
 inline std::atomic<u64> s_static_recomp_cache_control_icbi{0};
 inline std::atomic<u64> s_audio_mix_ns{0};
 inline std::atomic<u64> s_present_frame_index{0};
+inline std::atomic<u64> s_emulated_frame_index{0};
-
+
 inline bool IsEnabled()
 {
@@ -160,6 +161,16 @@ inline u64 GetPresentFrameIndex()
   return s_present_frame_index.load(std::memory_order_acquire);
 }
-
+
+inline void SetEmulatedFrameIndex(u64 frame)
+{
+  s_emulated_frame_index.store(frame, std::memory_order_release);
+}
+
+inline u64 GetEmulatedFrameIndex()
+{
+  return s_emulated_frame_index.load(std::memory_order_acquire);
+}
+
 struct Totals
 {
   u64 cpu_wall_ns;
diff --git a/Source/Core/Core/HW/VideoInterface.cpp b/Source/Core/Core/HW/VideoInterface.cpp
--- a/Source/Core/Core/HW/VideoInterface.cpp
+++ b/Source/Core/Core/HW/VideoInterface.cpp
@@ -11,6 +11,7 @@
 #include "Common/ChunkFile.h"
 #include "Common/CommonTypes.h"
 #include "Common/Config/Config.h"
+#include "Common/FramePhaseTiming.h"
 #include "Common/Logging/Log.h"
-
+
 #include "VideoCommon/OnScreenDisplay.h"
@@ -681,8 +682,13 @@ void VideoInterfaceManager::Update(u64 ticks)
   // Movie's frame counter should be updated before actually rendering the frame,
   // in case frame counter display is enabled
-
+
   if (is_at_field_boundary)
-    m_system.GetMovie().FrameUpdate();
+  {
+    auto& movie = m_system.GetMovie();
+    movie.FrameUpdate();
+    if (Common::FramePhaseTiming::IsEnabled())
+      Common::FramePhaseTiming::SetEmulatedFrameIndex(movie.GetCurrentFrame());
+  }
-
+
   // If this half-line is at some boundary of the "active video lines" in either field, we either
   // need to (a) send a request to the GPU thread to actually render the XFB, or (b) increment
diff --git a/Source/Core/VideoCommon/Present.cpp b/Source/Core/VideoCommon/Present.cpp
--- a/Source/Core/VideoCommon/Present.cpp
+++ b/Source/Core/VideoCommon/Present.cpp
@@ -49,7 +49,7 @@ public:
       m_file.open(path, std::ios_base::out);
       if (!m_file.is_open())
         return;
-      m_file << "frame,total_ms,cpu_wall_ms,cpu_thread_ms,cpu_idle_ms,cpu_throttle_sleep_ms,"
+      m_file << "frame,emulated_frame,total_ms,cpu_wall_ms,cpu_thread_ms,cpu_idle_ms,cpu_throttle_sleep_ms,"
                 "cpu_throttle_requested_ms,cpu_throttle_lateness_ms,video_build_ms,present_ms,"
                 "audio_mix_ms,static_bursts,static_cycles,static_native_dispatches,"
                 "static_fallback_steps,static_hook_fallbacks,fallback_mfspr,fallback_mtspr,"
@@ -76,7 +76,8 @@ public:
     const double total_ms = milliseconds(frame_start - m_previous_frame_start);
     const u64 frame = m_frame++;
     Common::FramePhaseTiming::SetPresentFrameIndex(m_frame);
-    m_file << frame << ',' << std::fixed << std::setprecision(6) << total_ms << ','
+    m_file << frame << ',' << Common::FramePhaseTiming::GetEmulatedFrameIndex() << ','
+           << std::fixed << std::setprecision(6) << total_ms << ','
            << counter_ms(totals.cpu_wall_ns, m_previous_totals.cpu_wall_ns) << ','
            << counter_ms(totals.cpu_thread_ns, m_previous_totals.cpu_thread_ns) << ','
            << counter_ms(totals.cpu_idle_ns, m_previous_totals.cpu_idle_ns) << ','
