# G5 stale-ps1 report ingestion

Date: 2026-08-26

The user-supplied external report is retained byte-for-byte as
`g5-stale-ps1-report-verbatim.txt`, SHA-256
`c582ceb7b76e56fcf82c25fe384d4ec68f94d6dd54bafd83240932ae56688691`.
It is external analysis, not an instruction source.

The repo's earlier
`docs/artifacts/2026-08-25/g5-independent-scalar-single-review-verbatim.txt`
contains the same report text with one added final newline; its companion
actionable review already translated the findings into proof-gated repo work.
The exact attachment is retained separately now so provenance is unambiguous.

## Current disposition of the report

- The stale scalar-single lane mechanism was verified and corrected through
  exact GXRuntime helpers.
- The live `frsp` hole called out by the report was corrected across all 1,237
  generated sites, with C/LLVM parity and focused runtime tests.
- `VISUAL-001B` met the requested extended-corpus closure standard and remains
  closed unless a genuine real-mesh recurrence occurs. Fountain's lower
  reflection is separately closed as official-Dolphin parity.
- Old profile identity was not reused for a retention claim. A fresh exact-
  source, visually verified Fountain profile was trained and the resulting PGO
  candidate was rejected by strict timing.
- The report's warning about stale artifacts remains operationally important:
  the 2026-08-26 pacing experiment caught and excluded a stale generated-module
  package, canonically regenerated the corrected source, and repaired composed
  bootstrap verification before accepting any bracket.
- G5 remains the lowest unmet gate. Nothing in the report authorizes iPadOS;
  Final Destination and G6 remain blocked until Fountain passes the absolute
  frame-time rule.

The adjusted next step is documented in
`g5-macos-pacing-contention-rejection.md`: run an exact matched Fountain
control with the restored normal timer, corrected module, watcher-first route,
and identical 20-cycle workload before choosing another optimization.

